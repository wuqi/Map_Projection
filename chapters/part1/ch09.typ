= 结论与资源

== 总结

#grid(
  columns: (1fr,2.8fr),
  column-gutter: 10pt,
  [

    关于地图投影的分类，已有多种不同的方法。大多数分类是正交的，因此任何单个投影可能同时属于不同类别。在其他分类方法中，如受生物学启发的毛雷尔分类法，则采用了分支分类体系。

    请注意，为便于定义和可视化，下文将根据经纬网对某些类别和投影进行非正式描述，因此某些特性可能看似依赖于地图所使用的特定投影方向。例如，圆柱投影中的坐标线在赤道投影中正交，但在极地或斜轴投影中则不正交，尽管所有其他属性仍然保持；毕竟，坐标网格只是一组常规线条。
  ],
    figure(
    image("../../img/image_1625494548866_0.png",width: 100%),
    caption: [根据兰伯特方位等积投影的赤道形式呈现的世界。未投影的原始卫星数据由NASA提供]
  ),

)

=== 按几何特征分类的投影

#table(
  columns: (auto, 1fr),   // 第一列自适应标题，第二列占据剩余空间
  inset: 10pt,            // 单元格内边距
  align: horizon,         // 内容垂直居中
  stroke: 0.5pt + gray,   // 边框粗细与颜色
  fill: (col, row) => if row == 0 { gray.lighten(80%) }, // 表头背景色

  // 表头
  [*类别*], [*特征*],

  // 内容行
  [方位投影], [亦称天顶投影。显示从单个点出发的真实方向（方位角）；在极地方位中，所有纬线为圆形，经线为直线，等距分布且汇聚于一点；未裁剪的世界地图为圆盘形。],
  
  [圆柱投影], [类比于圆柱作为中间投影面而定义；在赤道投影中，所有纬线和经线均为直线；经线与纬线正交且等距分布；未裁剪的世界地图为矩形。],
  
  [圆锥投影], [类比于圆锥作为中间投影面；在极地投影中，所有纬线为同心圆弧，经线为垂直于每条纬线的直线，等距分布且间距小于地球上的实际间距；未裁剪的地图为扇形或圆环形。],
  
  [伪圆柱投影], [在赤道投影中，所有纬线为平行的直线；经线为任意曲线，沿每条纬线等距分布。],
  
  [伪圆锥投影], [在极地投影中，所有纬线为同心圆弧，经线为任意曲线。],
  
  [任意投影或\ 折衷投影], [经纬线为任意曲线；通常无纯粹的几何构造定义。有些作者将任何非源自几何方法、而是为特定目的量身定制的投影称为"任意"、"常规"或"折衷"投影。],
)

从某种意义上说，圆锥作为极端情况包括圆柱（顶点在无穷远处的圆锥）和平面（高度为零的圆锥）。因此，圆锥投影组概括了方位投影和圆柱投影，并在广义上包括了伪圆柱和伪圆锥投影。此外，有些人认为多圆锥投影组包括那些纬线源自圆形的投影，包括改良的方位投影，如哈默投影和艾托夫投影。实际上，许多所谓的"方位"、"圆锥"或"圆柱"投影并非基于纯粹的几何体投影过程构建，而是由于映射坐标网格的几何特性而被如此分类。

此外，投影式、几何式或透视式投影可以精确类比于将原始表面连接到地图表面的光线几何设置。有些作者将其他投影称为"数学投影"。

=== 按属性特征分类的投影

#figure(
  grid(
    columns: (1.2fr, 1fr), // 左侧图片稍宽，比例可根据实际图片微调
    column-gutter: 8pt,
    
    // --- 左侧：单张大图 ---
    image("../../img/image_1625494960664_0.png", width: 100%),

    // --- 右侧：上下两张小图 ---
    grid(
      columns: 1,
      row-gutter: 8pt,
      image("../../img/image_1625494973198_0.png", width: 100%),
      image("../../img/image_1625494978485_0.png", width: 100%),
    )
  ),
  caption: [在方位投影中，从投影中心（可能与地图中心重合，也可能不重合）辐射出的直线之间的角度，与地球上对应线之间的角度相同。在左侧的方位等距地图上，沿这些线的距离也与地球上的距离成正比。
在等积正弦曲线地图（右上）上，对于任意两个相同的边界（如蓝色方块），其在地球上的对应部分将围成相同的面积，尽管它们不一定具有相同的形状。该伪圆柱投影仅沿两条轴线（绿色）保持角度不变；它仅沿这些轴线和所有垂直于短轴的线（红色）保持等距。
与几乎所有其他等角投影不同，艾森洛尔投影（右下）在每个点都保持小角度不变：绿色线在地球和地图上均垂直。面积和距离变形较大，但小于典型的等角投影。],
)

#table(
  columns: (auto, 1fr),   // 第一列自适应标题，第二列自动换行
  inset: 10pt,
  align: horizon,
  stroke: 0.5pt + gray,
  fill: (col, row) => if row == 0 { gray.lighten(80%) },

  // 表头
  [*类别*], [*特征*],

  // 内容行
  [等积投影], [地图上任何区域的面积与球面上对应区域的面积成正比；亦称等面积或等积。通常更适用于统计比较和教学目的。],
  
  [等距投影], [在地图上存在两组点 A 和 B，使得沿选定的线集（不一定是直线），从 A 中任一点到 B 中另一点的距离与球面上对应点之间沿这些对应线的距离成正比。换言之，在这些线上比例尺恒定，这些线称为标准线。],
  
  [等角投影], [在地图上任何小区域内，两条相交线与球面上对应线具有相同的角度，因此形状在局部得以保持。亦称正形或保角。对于导航目的和大比例尺制图最为重要。],

  // 跨列备注项：使用 colspan 合并两列
  table.cell(colspan: 2, fill: orange.lighten(95%))[
    #set text(size: 0.9em, style: "italic")
    注：在几乎所有等角投影中，至少有一个点（常选极点）要么无法表示，要么不满足等角性。
  ],

  [非等性投影], [有些作者用此名称指那些既不等角也不等积的投影。],
)

=== 投影概览


下文所列投影均有更详细的描述。这只是现有设计中的一小部分样本，未必是最重要或最常用的；因此，选择必然带有主观性。同样主观的是，改变投影方向或其他细节是否足以将其单独列为一个条目（例如，卡西尼投影与等距圆柱投影、高斯横轴投影与墨卡托投影、彼得曼投影与贝格豪斯投影之间的区别）。


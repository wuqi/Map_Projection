= 地理坐标系与投影坐标系

在地理信息系统（GIS）与制图学中，理解 GCS 与 PCS 的区别是处理空间数据的基石。其核心矛盾在于*不可展开表面（球面）与平面之间的几何冲突*。

== 定义

#let definition(title, body) = block(
  fill: luma(245),
  inset: 12pt,
  radius: 4pt,
  stroke: (left: 3pt + blue.darken(20%))
)[
  #text(weight: "bold", fill: blue.darken(20%))[#title] \
  #set text(size: 0.95em)
  #body
]

#definition("地理坐标系 (GCS)", [

  描述“圆”的球体参考

  基于三维球面或椭球体模型的参考框架，用于定义地球表面的真实空间位置。它通过经纬度的角度测量值，将不规则的地球表面拟合为规则几何形体，提供统一的全球参照基准。
])

#v(0.5em)

#definition("投影坐标系 (PCS)", [

  描述“平”的数学映射

  建立在地理坐标系之上的二维平面系统，通过数学变换将三维曲面映射为直角坐标平面。它将角度单位转换为线性单位（米/英尺），虽引入了几何变形，但极大地方便了地图测量与工程计算。
])

===  核心定义与维度逻辑

- #strong[地理坐标系 (Geographic Coordinate System, GCS)]
  - *维度本质*：基于#strong[三维椭球体]模型。
  - *定位机制*：使用角度测量值（经度 $lambda$ 与纬度 $phi$）描述点在弯曲表面上的位置。
  - *关键组件*：基准面 (Datum)、参考椭球体 (Spheroid) 及本初子午线。
  - *局限性*：无法直接在平面上进行面积或精确距离的线性测量。

- #strong[投影坐标系 (Projected Coordinate System, PCS)]
  - *维度本质*：基于#strong[二维平面]模型。
  - *实现逻辑*：通过数学变换（即地图投影）将 GCS 的球面坐标“摊平”。
  - *依赖关系*：#underline[每一个 PCS 都必然包含一个底层的 GCS]。
  - *核心参数*：投影算法（如 Mercator, UTM）、中央经线、标准纬线、线性单位（米/英尺）。

===  关键属性对比表

#table(
  columns: (1.2fr, 2fr, 2fr),
  inset: 10pt,
  align: horizon,
  stroke: none,
  fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
  
  [*特性*], [*地理坐标系 (GCS)*], [*投影坐标系 (PCS)*],
  [物理模型], [椭球体或球体（三维空间）], [笛卡尔坐标平面（二维）],
  [计量单位], [角度单位（度/分/秒/弧度）], [线性单位（米/公里/英尺）],
  [坐标表示], [经度 (Long) / 纬度 (Lat)], [东距 (Easting) / 北距 (Northing)],
  [投影变形], [无（真实反映球面几何）], [必然存在（形状、面积或长度）],
  [运算场景], [全球范围定位、大地测量], [工程测绘、导航、面积统计],
)

===  级联演变关系

坐标系的转换遵循以下层级流向：
$ "真实地球" arrow.r.double "参考椭球体" (#strong[GCS]) arrow.r.double "地图投影" arrow.r.double "平面地图" (#strong[PCS]) $

1. #strong[拟合]：首先通过“基准面”将不规则的地球表面拟合为规则的椭球体（如 WGS84）。
2. #strong[映射]：利用数学公式将球面上的点影射到可展开表面（圆柱、圆锥或平面）。
3. #strong[展平]：将可展开表面切开并铺平，形成直角坐标系统。

===  典型工业实例

- #strong[常用 GCS]：
  - *WGS 1984*：全球卫星定位系统 (GPS) 的标准。
  - *CGCS 2000*：中国当前的国家大地坐标系。
- #strong[常用 PCS]：
  - *UTM (通用横轴墨卡托)*：全球通用的 6 度分带投影，常用于卫星影像。
  - *Web Mercator*：Google Maps 和高德地图等互联网地图的在线显示标准。
  - *高斯-克吕格 (Gauss-Krüger)*：中国 1:50 万及更大比例尺地形图的法定投影。

#block(
  fill: rgb("#6fb7c4").lighten(95%),
  stroke: 0.5pt + rgb("#6fb7c4").lighten(50%),
  inset: 15pt,
  radius: 4pt,
  width: 100%,
  breakable: true  // 关键参数：允许跨页断开
)[
      #set text(font: ("LXGW WenKai Mono GB", "KaiTi"), size: 10pt)
    #set par(first-line-indent: 0pt, leading: 0.6em)

    #align(center)[
      #text(font: ("LXGW Neo XiHei Plus", "SimHei"), size: 12pt, weight: "bold")[核心辨析：地理坐标系（GCS）并非地图投影]
      #v(0.8em)
    ]

  从严格的制图学逻辑来看，#strong[地理坐标系（GCS）不是一种地图投影]，而是一套位置定义的参照准则。

  1. “描述”与“转换”的本质区别
  - #strong[GCS（描述）]：本质是#strong[球坐标系统]。它通过经纬度（角度）定义点在三维椭球体上的位置。它回答的是：“这个点在地球表面的哪个方位？”
  - #strong[PCS（转换）]：本质是#strong[直角坐标系统]。它是通过数学函数将三维经纬度映射到二维平面的过程。它回答的是：“如何把这个点画在平面纸张上？”

  2. 视觉错觉的根源：默认的“伪投影”
  当我们在 QGIS 或 ArcGIS 中加载 EPSG:4326 (WGS 84) 数据时，屏幕上依然会出现一张矩形地图。这常使人误以为 GCS 自带投影属性。实际上，这只是软件为了可视化而进行的#strong[临时映射处理]：
  - 它将 *1度经度* 简单映射为屏幕上的 *1个单位横向长度*；
  - 它将 *1度纬度* 简单映射为屏幕上的 *1个单位纵向长度*。

  这种映射在数学上被称为 #strong[等距柱状投影 (Equirectangular Projection)]。换言之，你看到的平面效果是软件施加的“视觉补偿”，而非 GCS 自身的几何属性。

  3. 属性对比总结
  #table(
    columns: (1fr, 2fr, 2fr),
    stroke: none,
    gutter: 3pt,
    fill: (col, row) => if row == 0 { rgb("#2e6f97").lighten(80%) } else if calc.even(row) { white } else { rgb("#2e6f97").lighten(98%) },
    [*特性*], [*地理坐标系 (GCS)*], [*地图投影 (Projection)*],
    [几何本质], [三维曲面（椭球体）], [二维平面（地图）],
    [数学属性], [位置的#strong[度量标准]], [位置的#strong[函数变换]],
    [核心目的], [解决“定位”问题], [解决“展平”问题],
    [单位], [角度（度、分、秒）], [长度（米、英尺）],
  )
]


== 地理坐标系在软件中的显示

=== QGIS 中的 EPSG:4326 为何显示为平面？

在 *QGIS*（以及大多数 GIS 软件）中，当你查看使用 *EPSG:4326 (WGS 84)* 的数据时，屏幕上显示的确实是一个平面。这背后的逻辑如下：

==== 1. 默认的“伪投影”显示：等距柱状投影
虽然 EPSG:4326 定义的是球面经纬度（单位是“度”），但计算机显示器物理上是二维平面的，无法直接显示球体。为了在屏幕上呈现数据，QGIS 默认使用了一种最简单的几何映射方式——#strong[等距柱状投影 (Equirectangular Projection / Plate Carrée)]：

- #strong[映射逻辑]：它直接将经度映射为 $X$ 轴，将纬度映射为 $Y$ 轴。
- #strong[单位转换]：在软件看来，它把“1度”直接当作平面上的“1个单位”来画图。
- #strong[数学表达式]：
  $ x = lambda $
  $ y = phi $
  （其中 $lambda$ 是经度，$phi$ 是纬度）

因此，如果你没有为项目设置特定的投影，QGIS 会使用这个简单的线性映射。因为经度范围是 $[-180, 180]$，纬度范围是 $[-90, 90]$，所以你在画布上看到的总是一个 $2:1$ 的长方形。

==== 2. 视觉畸变：为什么它看起来“怪怪的”？
如果你在 EPSG:4326 下查看中国或高纬度地区（如北欧、加拿大），会发现地图看起来比平时看到的要#strong[扁平]很多：

- #strong[原因]：在真实地球球面上，纬线越往两极越短（极点处缩为一个点）；但在这种平面映射下，两极的纬线被强行拉得和赤道一样长。
- #strong[变形]：这种显示方式会产生巨大的#strong[面积和形状畸变]，除了赤道附近，大部分地区的几何形态都是失真的。

==== 3. QGIS 的“动态投影”机制 (OTF)
QGIS 处理坐标矛盾的核心能力在于其#strong[动态重投影 (On-the-Fly Projection)] 功能：

- #strong[项目坐标系 vs 图层坐标系]：你的原始图层存储的是 EPSG:4326 的经纬度，但 QGIS 屏幕右下角显示的是#strong[项目 (Project)] 的坐标系。
- #strong[自动转换]：如果你的项目坐标系设为 *EPSG:3857*（Web 墨卡托），QGIS 会在内存中实时计算，将经纬度投射到对应的平面坐标上显示，而不需要修改原始矢量文件。


== EPSG:3857投影坐标系的官方标准与事实标准的异同

在现代 Web GIS 领域，EPSG:3857（Web Mercator）是应用最为广泛的投影坐标系。然而，深入研究会发现它存在两个并行的定义版本：一个是测绘界在 2008 年正式收录的 *官方标准 (De Jure Standard)*，另一个则是以 Google Maps 为代表、在工程界广泛使用的 *事实标准 (De Facto Standard)*。理解这两者之间约 11 公里的细微差异，是处理高精度地图对齐问题的核心基础。

从设计初衷来看，事实标准追求的是 *算法的对称美学*。为了极大简化 Web 地图瓦片的切片算法，开发者将地球强制拉伸为一个完美的正方形。根据赤道周长计算，赤道半长（X 轴范围）约为 $20,037,508.34$ 米。为了让地图高宽相等（$1:1$），Y 轴也被强行截断在相同的数值。通过墨卡托公式反算，当 Y 轴取该值时，对应的地理纬度恰好约为 $plus.minus 85.051129^deg$。这就是为什么所有主流 Web 地图切片服务在南北极方向都有一个特定的理论极限值。

相比之下，官方标准（由 IOGP 维护的 EPSG 注册中心定义）在收录此投影时，并未受到切片算法的束缚，而是倾向于 *地理描述的完整性*。官方将该投影的有效适用范围定义为整数倍的 $plus.minus 85.06^deg$。虽然这在数值上仅比事实标准的纬度多了约 $0.009^deg$，但由于墨卡托投影在接近极点时具有指数级的拉伸效应，这微小的差异映射到平面坐标上时，就会在垂直方向上多出约 $11,457.7$ 米。

这种 11 公里的差异导致了一个尴尬的现实：如果你严格按照 EPSG 官方给出的边界数值来渲染地图，你的世界地图将呈现为一个微弱的长方形，从而无法与标准的 Google 或 OpenStreetMap 瓦片实现像素级的完美对齐。

#table(
  columns: (1fr, 1.5fr, 1.5fr),
  inset: 10pt,
  align: horizon,
  stroke: none,
  fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
  [*对比维度*], [*事实标准 (Web Tile 逻辑)*], [*官方标准 (EPSG 定义)*],
  [几何形状], [严格正方形 ($1:1$)], [微长方形],
  [投影边界 (Y轴)], [$plus.minus 20,037,508.34$ 米], [约 $plus.minus 20,048,966.1$ 米],
  [截断纬度], [约 $plus.minus 85.051129^deg$], [严格 $plus.minus 85.06^deg$],
  [应用偏好], [工程易用性、切片对称], [地理范围完整、取整习惯],
)

对于开发者而言，在进行坐标转换或地图切片时，*必须以事实标准 ($85.051129^deg$) 为物理截断线*。EPSG 官网上的数值更多应被视为一种描述性的地理范围界定，而非工程实现的硬性指标。本质上，EPSG:3857 是一场用“球体计算公式”处理“椭球体原始数据”的数学折中，理清这 11 公里的源头是解决所有地图偏移问题的钥匙。

// 1. 设置代码块的字体（建议使用等宽字体，如 Fira Code, Consolas）
#set raw(theme: none) // 如果你想完全自定义颜色，可以关闭内置主题
#show raw: set text(font: "LXGW WenKai Mono GB", size: 9pt, fill: rgb("#2d5a27"))

// 2. 设置带背景的容器样式（针对多行代码块）
#show raw.where(block: true): it => block(
  fill: luma(245),      // 淡灰色背景
  inset: 10pt,          // 内边距
  radius: 4pt,          // 圆角
  width: 100%,
  stroke: 0.5pt + gray, // 边框
  it
)

  == 高斯-克吕格投影下坐标规律详解

  根据测绘实践规范，在我国常用的北京54、西安80及大地2000坐标系中，采用高斯-克吕格投影的平面坐标（x, y）整数部分遵循严谨的位数规律。

  === 核心结论：坐标的常见位数
  - *x 坐标（横坐标，东向位移）*：通常为 *6位* 或 *8位*。
  - *y 坐标（纵坐标，北向位移）*：通常为 *7位*。

  === 原理剖析：位数是如何产生的？
  理解这个规律的关键在于了解投影坐标为了避免负值而进行的“平移”处理。

  - *纵坐标 (y) 的 7 位数规律*：
    y 值表示从赤道起算到该点的*北向距离*（单位为米）。我国位于北半球，赤道到我国最南端约 3000 多公里，最北端超过 5000 公里。因此，y 值范围大致在几百万米之间，对应到整数部分即为 *7 位数*。

  - *横坐标 (x) 的 6 位或 8 位数规律*：
    x 值表示该点距离中央经线的*东向位移*。为了避免负值，规定将每带坐标原点向西*平移 500 公里*（即增加 500,000 米）。
    - *6 位数*：在 6 度带内，平移后的 x 值范围约在 167,000 至 833,000 米之间，因此整数部分通常为 *6 位*。
    - *8 位数*：当在 x 值前直接冠以 *2 位带号* 时，位数随之改变。例如第 20 带的 x 值 544,796 会表示为 20544,796，此时整数部分变为 *8 位*。

  === 实例验证：坐标表示方法对比

  #table(
    columns: (1.5fr, 2fr, 2fr, 1.2fr),
    inset: 10pt,
    align: horizon,
    stroke: none,
    fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
    [*表示方法*], [*x 坐标示例 (东向)*], [*y 坐标示例 (北向)*], [*位数分析*],
    [6度带 (不含带号)], [544796.468998], [4041212.357787], [x为6位, y为7位],
    [6度带 (含带号)], [20544796.468998], [4041212.357787], [x变为8位],
    [3度带 (不含带号)], [544796.468998], [4041212.357787], [x为6位, y为7位],
    [3度带 (含带号)], [39544796.468998], [4041212.357787], [x变为8位],
  )

  *注意*：此例中 3 度带与 6 度带数值相同系因选点特殊（中央经线重合），常规情况下数值不同但位数规律一致。

  === 重要补充：坐标轴对应关系
  在实际应用中，必须注意测绘领域与数学领域的定义差异：
  - *测绘与 GIS 领域*：通常以 *x 轴对应东向位移*，*y 轴对应北向位移*。
  - *数学领域*：习惯以 x 为横轴（水平），y 为纵轴（垂直）。

  这种定义差异可能导致在软件输入时需要调换顺序。务必在使用工具前确认其坐标轴定义，以防数据位置发生错误偏移。


= WKT-CRS 对投影的描述

WKT 不仅仅是文本，它是一套递归嵌套的“数学转换说明书”。


== WKT-CRS 的完整描述 (WKT2 标准)

在测绘与 GIS 领域，#strong[WKT-CRS] (Well-Known Text for Coordinate Reference Systems) 是交换坐标参考系统定义的通用语言。它将复杂的地理数学参数结构化，使不同软件系统能够精确理解坐标的物理含义。

一个完整的投影坐标系 (PROJCRS) 通常由以下递归嵌套的组件构成：

#strong[地理参考 (BASEGEOGCRS)]：定义投影所依赖的底层地理坐标系，包括参考椭球体 (ELLIPSOID) 和大地基准面 (DATUM)。

#strong[转换方法 (CONVERSION)]：声明所使用的数学模型。例如，使用“横轴墨卡托 (Transverse Mercator)”还是“兰伯特投影 (Lambert Conic Conformal)”。

#strong[投影参数 (PARAMETER)]：针对特定方法的数值设置，如中央经线 (False Easting)、标准纬线 (Standard Parallel) 和比例因子 (Scale Factor)。

#strong[坐标轴定义 (CS & AXIS)]：规定坐标的维度顺序（如 E,N 或 N,E）以及计量单位（如米）。


== 核心结构示例：拆解 EPSG:4526

我们将以中国大比例尺测绘常用的 #strong[EPSG:4526]（CGCS2000 / 3-degree Gauss-Kruger zone 38）为例，拆解其 WKT 描述中的核心信息：

```wkt
  PROJCRS["CGCS2000 / 3-degree Gauss-Kruger zone 38",
    BASEGEOGCRS["China Geodetic Coordinate System 2000",
      DATUM["China Geodetic Coordinate System 2000",
        ELLIPSOID["CGCS2000", 6378137, 298.257222101]]],
    CONVERSION["3-degree Gauss-Kruger zone 38",
      METHOD["Transverse Mercator"],
      PARAMETER["Latitude of natural origin", 0],
      PARAMETER["Longitude of natural origin", 114],
      PARAMETER["Scale factor at natural origin", 1],
      PARAMETER["False easting", 38500000],
      PARAMETER["False northing", 0]],
    CS[Cartesian, 2],
      AXIS["northing (N)", north],
      AXIS["easting (E)", east],
      LENGTHUNIT["metre", 1]]
```

=== WKT 核心信息详细拆解

==== 地理参考 (`BASEGEOGCRS`) - 大地基准与椭球体

```wkt
  BASEGEOGCRS["China Geodetic Coordinate System 2000",
      DATUM["China Geodetic Coordinate System 2000",
        ELLIPSOID["CGCS2000", 6378137, 298.257222101]]
```

-   *含义*：这部分定义了“地球”长什么样以及坐标原点在哪里。它是投影变形前的原始球面坐标系。
-   *椭球体 (`ELLIPSOID`)*：
    -   `["CGCS2000", 6378137, 298.257222101]`：
        -   *6378137*：这是*长半轴 (a)*，单位是米。有趣的是，这个数值和 GPS 使用的 WGS84 椭球体长半轴完全一致。
        -   *298.257222101*：这是*扁率分母 (f)*。通过这个数值可以计算出椭球的扁平程度。CGCS2000 的椭球扁率比 WGS84 略精细（WGS84 的扁率分母约为 298.257223563），这也是两者在大地水准面上细微差异的来源。
-   *大地基准面 (`DATUM`)*：
    -   `"China Geodetic Coordinate System 2000"`：这是中国国家大地坐标系，简称为 *CGCS2000*。它表示这个坐标系的原点位于地球质心，并且定向与 ITRF97 (国际地球参考框架) 一致。
    -   *应用意义*：意味着基于这个投影的坐标，是严格定义在地心、且经过中国地区精密修正的地球模型上的。

==== 转换方法 (`CONVERSION`) - 数学映射

```wkt
  CONVERSION["3-degree Gauss-Kruger zone 38",
      METHOD["Transverse Mercator"]
```

-   *含义*：这是从球面到平面的“魔法公式”。这里使用的是*高斯-克吕格投影*，它在数学上等价于*横轴墨卡托投影*。
-   *3度带*：说明这是按经度每3度划分一个投影带。中国大比例尺地形图（1:1万及以上）通常使用3度带，以保证边缘变形极小。
-   *zone 38 (第38带)*：这是带号。

==== 投影参数 (`PARAMETER`) - 约束变形的关键数值

这是理解投影后坐标数值的关键所在。

```wkt
    PARAMETER["Latitude of natural origin", 0]   // 原点纬度
    PARAMETER["Longitude of natural origin", 114] // 中央经线
    PARAMETER["Scale factor at natural origin", 1] // 中央经线比例因子
    PARAMETER["False easting", 38500000]           // 东偏移
    PARAMETER["False northing", 0]                  // 北偏移
```

-   *`Latitude of natural origin = 0`*：
    -   投影的起点纬度是赤道。虽然中国在北半球，但投影计算的数学原点选在赤道与本带中央经线的交点。
-   *`Longitude of natural origin = 114`*：
    -   *中央经线* = 114°E。
    -   *计算依据*：对于3度带，中央经线的计算公式为：*带号 × 3*。
    -   验证：38 × 3 = 114。完全匹配。这表示这个投影带覆盖的区域是中央经线114°左右各1.5度的范围（即东经112.5° ~ 115.5°），涵盖了湖北大部、湖南东北部、江西北部等地区。
-   *`Scale factor at natural origin = 1`*：
    -   *含义*：在中央经线（114°E）上，投影后长度没有变形（比例因子为1）。
    -   *效果*：离开中央经线越远，变形越大。因为是3度带，带宽只有约330公里，边缘的最大长度变形约为 1/1000 的量级，非常适合大比例尺测图。
-   *`False easting = 38500000` (东偏移 38,500,000 米)*：
    -   *核心逻辑*：为了避免出现负坐标，且能从数字中直接读出带号。
    -   *标准公式*：*带号 × 1,000,000 + 500,000*
    -   *推导*：带号 38 × 1,000,000 = 38,000,000；再加上 500,000 米（为了让中央经线对应的横坐标不是0，而是500公里）。
    -   *结果*：38,000,000 + 500,000 = *38,500,000*。
    -   *实战意义*：当你看到一个坐标为 (X: 38524567.89, Y: 3345678.12) 时：
        -   横坐标 (Easting) 的前三位 *385* 立刻告诉你，这个点位于*第38带*。
        -   该点距离中央经线 (114°E) 的*实际水平距离*为 524,567.89 - 500,000 = *24,567.89 米*（向东为正）。
-   *`False northing = 0`*：
    -   北半球（中国）没有使用南半球的偏移，所以赤道处的纵坐标就是 0 米。中国的纵坐标都是正数，且数值较大（如 334 万米，表示距离赤道约 3340 公里）。

==== 坐标轴与单位 (`CS & AXIS`)

```wkt
CS[Cartesian, 2],
    AXIS["northing (N)", north],
    AXIS["easting (E)", east],
    LENGTHUNIT["metre", 1]
```

-   *含义*：定义了平面直角坐标的组成。
-   *轴序 (`AXIS`)*：
    -   这里是 *(N, E)* 即 *(北坐标, 东坐标)*。
    -   *注意*：这是一个容易踩坑的地方。虽然我们通常口头说 “XY”，但在 CGCS2000 投影坐标的官方定义中，第一个坐标是 *Y (Northing/北方向)*，第二个坐标是 *X (Easting/东方向)*。在 GIS 软件（如 ArcGIS Pro, QGIS）中，如果坐标顺序搞反，数据会显示在错误的位置（通常会飞到非洲或者海里）。
-   *单位 (`LENGTHUNIT`)*：`["metre", 1]` 表示所有坐标值的单位都是米。

  == 制图学中 X 轴的定义：大地测量学传统与计算机图形学的协调

  === 定义分歧的起源：大地测量学与计算机图形学的不同范式

  在坐标系的使用中，X 轴的定义存在两种截然不同的传统，这种分歧源于应用领域的根本差异。

  *大地测量学传统：以方位角计算为核心的北-东系* 

  大地测量学（Geodesy）和经典测量学（Surveying）的核心任务之一是精确计算方位角。为简化计算，该领域定义了与数学笛卡尔坐标系不同的轴向：
  - #strong[X 轴（第一轴）]：指向#strong[正北方向]，坐标值称为#strong[北向坐标（Northing）]，表示从赤道起算的纬度方向距离。
  - #strong[Y 轴（第二轴）]：指向#strong[正东方向]，坐标值称为#strong[东向坐标（Easting）]，表示在投影带内距中央经线的偏移量。
  该传统深刻影响了中国国家坐标系（如北京 54、西安 80）的定义方式，即坐标对存储顺序为（X 北向，Y 东向）。

  *计算机图形学传统：以视觉呈现为核心的东-北系* 

  计算机图形学与 GIS 软件的显示引擎遵循标准的屏幕直角坐标系，旨在符合“上北下南、左西右东”的视觉直觉：
  - #strong[水平轴（通常标记为 X）]：向右为正，对应#strong[东向]。
  - #strong[垂直轴（通常标记为 Y）]：向上为正，对应#strong[北向]。
  因此，在 QGIS、ArcGIS 等软件的状态栏中显示的 X 值实为东向坐标，而 Y 值为北向坐标。

  === WKT 标准对坐标轴定义的规范化

  为消除数据交换中因轴向定义歧义导致的坐标错乱问题，开放地理空间信息联盟（OGC）制定的 WKT（Well-Known Text）标准不断演进。

  *WKT1 的局限性* 

  早期版本（如 ESRI WKT）对坐标轴的定义往往缺失或仅以文本形式注明，缺乏机器可读的方向关键字。这导致软件在解析时需依赖默认假设，从而频繁出现横纵坐标互换的错误。

  *WKT2 (ISO 19162) 的严谨定义* 

  WKT2 强制要求显式定义每一坐标轴的方向，其核心语法为：
  `AXIS["轴名称", 方向关键字, ORDER[序号]]`

  - #strong[方向关键字]：采用标准化术语，如 `north`、`east` 等，为机器提供了明确的解析依据。
  - #strong[ORDER 序号]：指明该轴在数据存储中的物理顺序。

  例如，一个标准的 WKT2 定义如下：
  ```wkt
  PROJCRS["Example_Projected_CRS",
      CS[Cartesian, 2],
          AXIS["northing (X)", north, ORDER[1]],   # 第一轴名为X，方向北
          AXIS["easting (Y)", east, ORDER[2]]     # 第二轴名为Y，方向东
  ]
```
此定义明确告知软件：数据存储顺序为先北向后东向，无论轴名如何，方向是决定映射的依据。

=== 轴交换机制：从数据存储到屏幕显示的转换流程
现代 GIS 软件通过“轴交换”机制协调大地测量学与计算机图形学的定义分歧，其工作流程如下：
#strong[数据读取]：从文件（如 Shapefile）中读取坐标对，严格遵循 WKT 中定义的 ORDER 顺序，即先读取 ORDER[1]（北向坐标），后读取 ORDER[2]（东向坐标）。
#strong[方向解析]：投影库（如 PROJ）解析 WKT 中的 AXIS 定义，获取每一轴对应的地理方向。
#strong[映射变换]：显示引擎将数据映射到屏幕坐标系（向右为东，向上为北）。此时执行逻辑映射：
- 将#strong[数据的第二轴（方向东）]映射到#strong[屏幕 X 轴]；
- 将#strong[数据的第一轴（方向北）]映射到#strong[屏幕 Y 轴]。
此过程即“轴交换”：它并非修改数据存储顺序，而是改变轴在显示层面的角色。
#strong[最终呈现]：用户在状态栏看到的 #strong[X 值] 实际来源于数据的第二轴（东向），而 #strong[Y 值] 来源于数据的第一轴（北向）。
=== 轴定义歧义对坐标输入的影响
在实际应用中，用户常遭遇“坐标需颠倒输入”的现象，其根源在于不同软件对“X”输入框的定义存在差异：
#strong[基于测绘传统的软件]：其界面“X”输入框对应数据模型的第一轴（北向）。
#strong[基于 GIS 显示习惯的软件]：其界面“X”输入框对应屏幕横轴（东向）。
若用户持有的坐标对是（北向，东向）且未明确软件定义，极易导致输入错误。WKT2 的严谨定义正是为了从源头上消除此类歧义。
=== 总结：轴定义与映射的认知框架
#table(
  // 调整比例：1.8fr 改为 2.2fr，给最后一列更多空间
  columns: (0.8fr, 1fr, 0.6fr, 1.2fr, 2.2fr), 
  inset: 10pt,
  align: horizon,
  stroke: none,
  fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
  
  [层面], [轴标识], [方向], [代表值示例], [WKT2 角色],
  [数据存储], [第一轴], [北], [7位北向], [`AXIS[..., north, 1]`],
  [数据存储], [第二轴], [东], [6/8位东向], [`AXIS[..., east, 2]`],
  [屏幕显示], [横轴 (X)], [东], [6/8位东向], [映射自第二轴],
  [屏幕显示], [纵轴 (Y)], [北], [7位北向], [映射自第一轴],
)

#strong[结论]：WKT2 通过强制定义轴向方向，将坐标系定义的“命名权”与“方向权”分离。这一机制确保了全球数千种坐标系在跨平台交换中的一致性，是当代 GIS 互操作性的基石。

== 投影数学参数对比：高斯-克吕格 (圆柱) vs. Albers (圆锥)

#quote(block: true)[
*核心逻辑*：在 WKT 中，即使参数名（如 "False Easting"）完全相同，如果它们属于不同的投影方法，其背后的 *EPSG Parameter ID* 是完全不同的。ID 是确保投影数学公式正确运行的唯一“身份证”。
]

=== 核心数学参数对照表

#table(
columns: (1fr, 1.2fr, 1.2fr, 2fr),
inset: 10pt,
align: horizon,
stroke: none,
fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
[*物理意义*], [*高斯-克吕格*], [*Albers 圆锥*], [*差异深度解析*],
[投影方法 ID], [*9807* (TM)], [*9822* (Albers)], [横轴圆柱 vs. 正轴圆锥],
[中央经线 ID], [*8802* (Natural)], [*8822* (False)], [带中心 vs. 极坐标基准],
[起始纬度 ID], [*8801* (Natural)], [*8821* (False)], [赤道原点 vs. 圆锥顶点参照],
[比例因子 ID], [*8805* (Scale)], [不适用 (N/A)], [高斯靠此调节形变],
[标准纬线 ID], [不适用 (N/A)], [*8823* / *8824*], [圆锥特有的两条无变形割线],
[东伪偏移 ID], [*8806* (False)], [*8826* (False)], [#strong[严禁混用]：对应不同公式占位符],
[北伪偏移 ID], [*8807* (False)], [*8827* (False)], [#strong[严禁混用]：对应不同公式占位符],
)

=== 核心数学逻辑差异

*“自然原点”与“伪原点”之争* 

- #strong[高斯-克吕格 (8801/8802)]：使用的是 *Natural Origin*。投影圆柱体与地球相切，所有计算直接从赤道和中央经线的实际地理交点开始。
- #strong[Albers (8821/8822)]：使用的是 *False Origin*。圆锥投影的几何顶点往往是虚拟的，为了让平面坐标落在合理区间，它定义了一个“伪原点”作为平面转换的锚点。

* 控制形变的手段：比例因子 vs. 标准纬线* 

- #strong[高斯-克吕格：靠“乘法” (8805)]：通过给中央经线设定一个系数（如 1 或 0.9996），将投影中心的精度向边缘“稀释”，从而减小整体形变。
- #strong[Albers：靠“切割” (8823/8824)]：它不使用比例因子，而是通过定义两条#strong[标准纬线]（Standard Parallels）让圆锥面两次穿过地球。在这两条线上，比例因子严格等于 1.0。

=== WKT 源码片段对比示例

*高斯-克吕格 (典型圆柱投影)*
```wkt
METHOD["Transverse Mercator", ID["EPSG", 9807]],
PARAMETER["Longitude of natural origin", 105.0, ID["EPSG", 8802]],
PARAMETER["Scale factor at natural origin", 1.0, ID["EPSG", 8805]],
PARAMETER["False easting", 35500000.0, ID["EPSG", 8806]],
PARAMETER["False northing", 0.0, ID["EPSG", 8807]]
```

*Albers (典型圆锥投影)*

```wkt
METHOD["Albers Equal Area", ID["EPSG", 9822]],
PARAMETER["Latitude of 1st standard parallel", 25.0, ID["EPSG", 8823]],
PARAMETER["Latitude of 2nd standard parallel", 47.0, ID["EPSG", 8824]],
PARAMETER["Longitude of false origin", 105.0, ID["EPSG", 8822]],
PARAMETER["Latitude of false origin", 0.0, ID["EPSG", 8821]],
PARAMETER["False easting", 0.0, ID["EPSG", 8826]],
PARAMETER["False northing", 0.0, ID["EPSG", 8827]]
```

=== 总结
判断一个投影参数是否设置正确，不能只看名字，必须核对 ID。记住：8801-8807 系列属于“圆柱投影派”，而 8821-8827 系列属于“圆锥投影派”。两者在数学逻辑中完全不兼容。

== WKT2 (ISO 19162) 标准深度解析
=== WKT2 提出的必要性
WKT2 的出现并非简单的版本迭代，而是为了终结测绘界长达二十年的“方言乱象”并对接现代大地测量需求。
==== WKT1 的历史局限与实现分歧
WKT1（基于 OGC 99-049 或 01-009）在长期实践中暴露了三个致命缺陷：

- #strong[多重方言并存]：业界分裂为“OGC WKT”与“ESRI WKT”等多种互不兼容的变体。Apache SIS 等工具甚至需要维护三套以上的解析逻辑来兼容不同的软件厂商。

- #strong[单位解释歧义]：以本初子午线（PRIMEM）为例，规范对角度单位的继承关系描述模糊。这导致同一份定义在不同软件中解析时，可能产生高达 #strong[17 公里] 的位置偏差（以巴黎纬度计算）。

- #strong[轴向定义的缺失与误读]：WKT1 允许省略 AXIS 元素。许多软件因此强制采用（经度，纬度）顺序，这与 ISO 19111 标准中的（纬度，经度）约定经常发生冲突，导致地图出现“横纵颠倒”。

==== 大地测量学发展的新需求
随着高精度定位技术的普及，WKT1 已经无法承载以下高级特性：

- #strong[动态坐标参考系]：无法描述随时间变化的坐标系（如澳大利亚等板块运动活跃地区的坐标更新）。

- #strong[高精度转换模型]：传统的“WGS84 中转法”会引入约 2 米的误差，现代测绘需要支持“后期绑定”（late-binding）的直接转换描述。

- #strong[复杂转换链条]：WKT1 难以表达从一个坐标系到另一个坐标系之间多步骤、复合式的转换路径。
==== 标准化组织的应对
为了彻底解决上述矛盾，OGC 与 ISO/TC 211 联合启动了修订工作：

- #strong[2015 年]：发布 ISO 19162:2015（WKT-CRS 1.0），奠定了 WKT2 的基础。

- #strong[2019 年]：根据最新的坐标参考系概念模型，发布了 #strong[ISO 19162:2019]（WKT-CRS 2.0）。

这一系列新标准通过引入更严密的数学逻辑和强制性的元素声明，确保了地理空间数据在全球范围内的精确互操作性。

=== WKT2 与 WKT1 的系统性对比

==== 语法结构的演进

WKT2 不仅规范了术语，还通过强制性的格式要求消除了解析歧义。

#table(
  columns: (1fr, 1.5fr, 1.5fr),
  inset: 10pt,
  align: horizon,
  stroke: none,
  fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
  [*对比维度*], [*WKT1 (OGC 01-009)*], [*WKT2 (ISO 19162:2019)*],
  [顶层关键字], [GEOGCS, PROJCS, COMPD_CS 等], [GEODCRS, PROJCRS, COMPOUNDCRS 等],
  [元素命名], [使用简称（如 PRIMEM, SPHEROID）], [规范化命名（如 ELLIPSOID）],
  [括号风格], [允许方括号 `[]` 或圆括号 `()` 混用], [强制使用方括号 `[]`],
  [轴向表达], [AXIS["名称", 方向]], [AXIS["名称", 方向, ORDER, 单位]],
)

==== 轴向定义的强制性与精确性

轴向定义是 WKT2 最核心的改进，解决了地图“横纵颠倒”的历史顽疾。

- *WKT1 的缺陷*：`AXIS` 为可选元素，缺失时依赖软件默认假设，常与 ISO 标准冲突。
- *WKT2 的严谨性*：
  - 强制显式定义每一轴的#strong[方向]（north/east/up 等）。
  - 引入 `ORDER[序号]` 明确数据在内存/文件中的#strong[物理存储顺序]。
  - 轴名称仅作为标签，方向关键字才是机器解析的唯一依据。

```wkt
// WKT2 三维地理 CRS 示例：明确了经度、纬度、高度的顺序与单位
GEODCRS["WGS 84 (3D)",
  CS[ellipsoidal, 3],
    AXIS["Longitude (L)", east, ORDER[1], ANGLEUNIT["degree", 0.0174532925]],
    AXIS["Latitude (B)", north, ORDER[2], ANGLEUNIT["degree", 0.0174532925]],
    AXIS["Ellipsoidal height (h)", up, ORDER[3], LENGTHUNIT["metre", 1]]]
```
==== 单位机制的彻底澄清
WKT2 消除了由于上下文推断导致的单位误差（曾导致高达 17 公里的偏移）。
每个涉及数值的元素必须显式关联 LENGTHUNIT 或 ANGLEUNIT。
单位定义独立于上下文，不再依赖“默认约定”。
支持更丰富的单位类型，如 ScaleUnit（比例单位）和 TimeUnit（时间单位）。
==== 坐标操作的完整建模
WKT2 对坐标转换逻辑进行了革命性重构，从“黑箱转换”转向“透明建模”。
#table(
columns: (1fr, 1.2fr, 2fr),
  inset: 10pt,
  align: horizon,
  stroke: none,
  fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
[特性], [WKT1], [WKT2],
[定义方式], [独立于 CRS 的单一描述], [集成的 COORDINATEOPERATION 对象],
[源/目标], [隐含或通过上下文关联], [强制指定 SOURCECRS 与 TARGETCRS],
[地理范围], [无描述], [AREA 和 BBOX 明确适用地理区间],
[精度估计], [无], [OPERATIONACCURACY 提供预期精度],
)
==== 与概念模型的一致性
WKT2 严格对齐 ISO 19111 概念模型，解决了 WKT1 无法直接表达三维地理 CRS 的架构缺陷，并原生支持随时间变化的#strong[动态坐标参考系]。
==== 兼容性与迁移建议
虽然 WKT2 设计时考虑了向下兼容，但开发者需注意：
- TOWGS84 元素已被移除，由更严谨的 BOUNDCRS 机制替代。
- 坐标操作（转换公式）描述完全不兼容。
- 建议使用 Apache SIS 等库提供的兼容策略（如 WKT1_IGNORE_AXES）进行平滑过渡。

== 分瓣投影 在wkt中表示以及在gis软件中表示
分瓣投影（Interrupted Projection）作为一种为了减少大陆变形而将地图“切开”的特殊投影，其在标准化文本表示（WKT）和商业GIS软件中的支持情况，比常规投影复杂得多。

=== 核心概念澄清：分瓣 vs. 分带
在深入技术实现前，需明确区分两个极易混淆的概念：
*   *分瓣投影 (Interrupted Projection)*：将全球地图沿若干经线或特定路径“切开”，形成多个独立 lobe（瓣）。其目的是使每一瓣内的投影变形最小化，尤其关注大陆轮廓的保真度，如古德分瓣投影 。
*   *分带投影 (Zone-based Projection)*：如高斯-克吕格或UTM投影，将地球按固定经差（如6度）连续划分成带，每一带拥有独立的、数学定义明确的投影坐标系。这是为了在大范围区域内控制长度变形 。

*结论前置*：当前主流的WKT标准（包括WKT2）*没有为分瓣投影提供原生的、直接的表示方法*。其在GIS软件中的实现，依赖于各软件特有的底层库（如PROJ）或编程脚本。

=== WKT标准中的表示：为何“缺席”？
WKT（Well-Known Text）的核心功能是精确描述一个单一的、数学上连续的坐标参考系（CRS），包括其基准面、投影方法、参数以及坐标轴顺序 。分瓣投影的本质是由多个独立的投影片段拼接而成，并非一个单一的、全域连续的数学函数。

1.  *不符合WKT的建模对象*：一个分瓣投影地图，在数学上是由多个不同中央经线的投影（如多个正弦投影）组合而成的复合体。这与WKT旨在描述的“一个CRS”在概念上存在根本冲突。
2.  *缺乏标准化参数*：要定义一个分瓣投影，需要指定瓣的数量、每个瓣的边界经度、中央经线以及基础投影类型。这些复杂的、非结构化的元数据无法被标准的WKT语法容纳。
3.  *现有的近似表达*：你可能会在一些特定软件或库中看到如 `+proj=igh` (Interrupted Goode Homolosine) 这样的PROJ字符串参数 。但这属于特定库的扩展参数，而非通用的WKT标准。它表明，底层投影库（如PROJ）有能力在代码层面实现这种复合投影，但这并未反映在WKT的语法层面。

=== 在GIS软件中的表示与实现
由于缺乏WKT标准支持，GIS软件处理分瓣投影的方式呈现出多样化和技术依赖的特点。

=== PROJ库的“元投影”创新
近期（2025年）的开源社区动态表明，PROJ库（作为QGIS等软件的底层投影引擎）正在尝试一种新的解决方案。开发者Javier Jimenez Shaw提出并实现了一个名为 *“interrupted”的元投影* 。
*   *实现原理*：它作为一个“元投影”，接收两个核心参数：*基础投影*（如 `tmerc` 横轴墨卡托, `sinu` 正弦投影）和 *lobe定义*（即每个分瓣的范围）。
*   *技术意义*：这代表了一种在底层库层面统一处理分瓣投影的尝试，为未来QGIS等前端软件直接支持提供了可能。

=== 在主流GIS软件中的多种实现路径
目前，用户若想在GIS软件中使用分瓣投影，主要依赖以下几种方法：

#table(
  columns: (1fr, 1.5fr, 1.5fr),
  inset: 10pt,
  align: horizon,
  stroke: none,
  fill: (col, row) => if row == 0 { gray.lighten(40%) } else if calc.even(row) { gray.lighten(95%) },
  [*软件/工具*], [*表示与实现方式*], [*说明*],
  [#strong[QGIS]], [依赖PROJ库。未来可能通过PROJ的`interrupted`元投影直接选择 。], [目前需通过自定义CRS或插件实现。],
  [#strong[GDAL]], [通过`gdalwarp`等工具进行重投影，但需手动处理分瓣的切割与拼接 。], [高级用户可通过脚本组合多个投影实现。],
  [#strong[D3.js]], [在JavaScript中直接实现。开发者Jason Davies提供了多种分瓣投影的JavaScript实现 。], [前端可视化常用，但非桌面GIS软件。],
  [#strong[GMT]], [内置了分瓣正弦投影等支持 。], [科研制图领域的老牌工具，支持较为完善。],
  [#strong[R (mapproj)]], [通过`mapproj`包，支持包括正弦投影在内的多种投影，并可间接构建分瓣效果 。], [适用于统计分析流程中的地图绘制。],
  [#strong[商业软件]], [通常需要复杂的自定义或模拟。一篇指南曾讨论“用ArcInfo模拟分瓣古德投影” 。], [原生支持较弱，多通过分幅输出或脚本实现 。],
)

=== 总结

-  *WKT标准*：目前无法原生表示分瓣投影。因为它描述的是一个单一的、连续的坐标系，而分瓣投影本质上是多个投影片段的“拼接”。

-  *GIS软件*：支持程度不一，呈现“*底层库创新*”与“*前端多样化实现*”并存的局面。
    -  *底层趋势*：以PROJ库为代表的投影引擎正在通过“元投影”的概念，试图将分瓣投影作为一种可参数化的投影类型进行统一支持 。
    -  *应用现状*：用户在QGIS、ArcGIS等软件中直接使用分瓣投影仍有门槛，通常需要借助特定软件（如GMT）、编程库（如D3.js, R）或复杂的脚本工作流 。

因此，当你需要处理分瓣投影时，需要根据具体使用的软件或库，去查找其特有的实现方式（如PROJ的`+proj=igh`参数或未来的`+proj=interrupted`参数），而非寄希望于一个通用的WKT定义。对于GIS软件的日常交互，你可能仍需手动模拟或依赖专门的制图工具来生成分瓣投影地图。
